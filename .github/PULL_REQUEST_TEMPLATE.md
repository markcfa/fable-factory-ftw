## Entry: <project name>

**What I built:** <one or two sentences — sell it>

### Checklist

- [ ] One new file `entries/<slug>.json` — valid JSON, all required fields
- [ ] `voteIssue` and `starsAtEntry` left as `null` (the Action sets them after merge)
- [ ] Built substantially with Claude — `builtWith` says the model and surface
- [ ] Screenshot (optional) under `assets/`, ≤ 300 KB
- [ ] One project per PR
